@{
    ViewData["Title"] = "Submit Claims";
}
@model ST10384311PROG6212POE.Models.Entities.Claims

<!Doctype html>
<html>
<head>
    <link rel="stylesheet" href="~/css/submitclaims.css"/>
    <script src="~/js/submitclaims.js"></script>
</head>

<body>
    <div class="form-container">
        <h2>Submit Your Claim</h2>

        <!--A form that allows the Lecturer to Submit Claims-->
        <form asp-controller="Home" asp-action="SubmitClaims" method="post" enctype="multipart/form-data">
            <!-- Lecturer Information -->
            <div class="form-group">
                <label for="lecturerName">Name</label>
                <input type="text" id="lecturerName" name="LecturerName" placeholder="Enter your name" required />
            </div>

            <div class="form-group">
                <label for="lecturerEmail">Email</label>
                <input type="email" id="lecturerEmail" name="LecturerEmail" placeholder="Enter your email" required />
            </div>

            <!-- Claim Period -->
            <div class="form-group">
                <label for="claimPeriod">Claim Period</label>
                <input type="month" id="claimPeriod" name="ClaimPeriod" required />
            </div>

            <!-- Total Hours Worked -->
            <div class="form-group">
                <label for="totalHours">Total Hours Worked</label>
                <input type="number" id="totalHours" name="TotalHours" placeholder="Enter total hours" />
            </div>

            <!-- Supporting Documents -->
            <div class="form-group">
                <label for="supportingDocs">Upload Supporting Documents</label>
                <input type="file" id="supportingDocs" name="supportingDocs" />
            </div>

            <!-- Submit Button -->
            <button type="submit" class="btn-submit">Submit Claim</button>
        </form>

    </div>
</body>
</html>
<!---------------------------------------------------------------End Of File----------------------------------------------------->